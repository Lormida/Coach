/* exports.createCoach = function (req, res, next) {
  Coach.create(req.body.data)
    .then((data) => res.status(200).json({ status: 'success created', ...data }))
    .catch(err => res.status(401).json({ error: err }))
} */